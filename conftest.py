# Empty root conftest. Its presence marks the project root for pytest and
# guarantees this directory is on sys.path, so `import app...` resolves whether
# you run `pytest`, `pytest tests/`, or a single file.
